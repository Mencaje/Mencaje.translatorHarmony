# Mencaje 大文 Logo — 鸿蒙冷启动规格（可复用）

> 源图与本文档同目录：`01972998-78dc-483e-8d98-c84fa83ea6d3_removalai_preview.png`  
> 去背景 PNG，绿色「文」形 Logo，画布内已几何居中。  
> **仅显示正中 Logo，无底部品牌条。**

---

## 1. 源图与画布

| 项 | 数值 |
|----|------|
| 文件名 | `01972998-78dc-483e-8d98-c84fa83ea6d3_removalai_preview.png` |
| 工程资源 | `entry/.../media/splash_logo.png` |
| 画布尺寸 | **600 × 600 px** |
| 背景 | **透明**（Alpha=0） |
| 屏幕背景色 | **`#FF000000`**（纯黑） |

---

## 2. Logo 在画布内的像素坐标（非透明区域外接矩形）

| 项 | 像素 (px) | 相对画布比例 |
|----|-----------|----------------|
| 左边距 left | **85** | 14.17% |
| 上边距 top | **117** | 19.50% |
| 右边距（至右缘） | **85** | 14.17% |
| 下边距（至下缘） | **115** | 19.17% |
| Logo 宽 | **431** | 71.83% |
| Logo 高 | **369** | 61.50% |
| Logo 中心 X | **300** | 50.00% |
| Logo 中心 Y | **301** | 50.17% |

**外接矩形：** `(85, 117, 515, 485)`

---

## 3. 第一层与第二层统一规则（重要）

| 层 | 配置 | 显示规则 |
|----|------|----------|
| **第一层（系统冷启动）** | `startWindowIcon` = **`splash_logo`** | PNG **实际像素**、**屏幕正中**、**不拉伸** |
| **第二层（Splash 页）** | 同一张 **`splash_logo`** | `ImageFit.None` + `Stack` 居中，与第一层一致 |
| 背景 | `start_window_background` = `#FF000000` | 纯黑 |

```json5
// module.json5
"startWindowIcon": "$media:splash_logo",
"startWindowBackground": "$color:start_window_background",
```

```typescript
// Splash.ets（第二层须与系统层一致）
Stack({ alignContent: Alignment.Center }) {
  Image($r('app.media.splash_logo'))
    .objectFit(ImageFit.None)
}
.backgroundColor($r('app.color.ui_pure_black'))
```

**勿对第二层使用 `Cover` / `Fill` 或 `width('100%')` 缩放**，否则与第一层大小、位置不一致。

---

## 4. 本工程文件位置

```
entry/src/main/resources/base/media/
  splash_logo.png          ← 冷启动唯一图（600×600）

entry/src/main/ets/pages/Splash.ets
entry/src/main/ets/constants/SplashColdStartSpec.ets
entry/src/main/ets/entryability/EntryAbility.ets   → loadContent('pages/Splash')
entry/src/main/module.json5
```

---

## 5. 新项目复用步骤

1. 复制 `splash_logo.png`（600×600 透明）到 `entry/.../media/`
2. 配置 `module.json5` 的 `startWindowIcon` / `startWindowBackground`
3. 复制 `Splash.ets`（`ImageFit.None` + 居中），`EntryAbility` 首屏加载 `pages/Splash`
4. 编译安装 HAP

---

## 6. 坐标换算（换画布边长 S）

```
scale = S / 600
left'   = round(85  * scale)
top'    = round(117 * scale)
width'  = round(431 * scale)
height' = round(369 * scale)
```

---

## 7. 注意事项

- 不要用全透明 `startWindowIcon` 占位，易闪白屏。
- 改 Logo 大小：调整 **PNG 像素尺寸**（保持画布内 Logo 比例），两层仍用同一文件。
- `splash_start_composite` / `splash_brand_bottom` 已停用，可保留素材作备份。

---

## 8. 版本记录

| 日期 | 说明 |
|------|------|
| 2026-04-12 | 初版：正中 Logo 冷启动 |
| 2026-04-12 | 曾加底栏品牌条 + 合成图（已取消） |
| 2026-04-12 | 恢复仅正中 Logo；两层统一 `splash_logo` + `ImageFit.None` |
